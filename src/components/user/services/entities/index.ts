import {
  IUser,
  IUserWithTokens,
  UpdateUserBody,
  NewRegisteredUser,
  NewCreatedUser,
} from './user.entity';

export { IUser, IUserWithTokens, UpdateUserBody, NewCreatedUser, NewRegisteredUser };
